import { Typography } from '@mui/material';
import React from 'react';

const UserCases = ({ match }) => {

    return (
        <Box>
            <Typography>Mis casos</Typography>

        </Box>
    );
}

export default UserCases;
